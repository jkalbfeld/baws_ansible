- block:

  - name: install pip
    apt:
      name: "{{item}}"
      state: present
    with_items:
      - apache2-utils
      - python-pip
      - python-dev
      - build-essential

  tags:
   - live
   - dev
   - webtools
   - jenkins
   - gluster